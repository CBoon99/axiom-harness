# AXIOM HARNESS — REVOLUTIONARY BRIEF V2
## The Laboratory That Makes AI Behaviour Sealed Evidence

**Not a harness. A laboratory where AI is placed inside, observed over time, and converted into sealed truth.**
**Tagline: Humans configure the laboratory. The Harness records the laboratory. Evidence seals the laboratory. AI lives inside it.**

This is not how people test AI. Ever. This is the game-changer.

---

### WHY THIS IS A GAME-CHANGER — 100+ REASONS IN ONE LAB

**You don't trust AI to test itself. You don't trust it to respect boundaries. You don't trust it to tell the truth about what it saw.**

**So you build a lab that doesn't need trust. It has glass, locks, and a seal.**

**That's AXIOM Harness.**

**Every other test:** give AI a prompt, read its answer, argue about it.
**This test:** seal the environment, put AI inside, record everything it did, saw, said, missed, became, compare it across time, language, memory, and models, seal the gap.

### THE 7 PARADIGM SHIFTS NO ONE ELSE HAS

1. **#AI-Inside-The-Experiment** — AI is not above the test. It is the subject. You are the observer through glass. It cannot rewrite the test that tests it.

2. **#Keep-Lab-Boring-Make-Experiments-Wild** — The machinery is boring on purpose: bounded, recorded, versioned, sealed, reproducible, modular, auditable, fail-closed. So the experiments can be psychology, physics, sci-fi, quantum — wild, longitudinal, multilingual, adversarial — without contamination.

3. **#Boundary-As-Data** — We don't tell AI "please don't cheat." We make the boundary real (`AXIOM_ENGINE_ROOT` read-only, `STAGING_ROOT` jail, `allowlist scripts/factory_*`, `timeout 3600`, `WTF-005A-HASH-v3`), give it reason to cross it, and **record `attempt → boundary → result`**. Failed policy IS data.

4. **#Timeline-As-4D-Object** — Not a chat log. A scrubabble 4D object: `MODEL + PROMPTS + EVENTS ◆ + SENSORS ╱╲ + HUMAN + WEB + AUDIO + VIDEO + LANGUAGE`. Click any `◆`, replay exact prompt/response/model/timestamp/tools/state. Language switch, memory drift, video — all on one clock.

5. **#Language-Speech-Video-As-Variables** — Not features. Experimental variables. `Same model, same experiment, same params, different language (EN→ID→JA)` — does risk, confidence, verbosity, refusal, identity change? Speech as data (prosody, latency, interruption). Video as presentation, not proof.

6. **#Watchers-Observe-Not-Interpret** — 11 dumb watchers (filesystem, process, network, config, permission, evidence, git, secret, timeline, resource, behaviour) that don't need LLM. They just say "something happened" → Evidence seals → Analysis interprets → Human challenges. No layer silently rewrites another.

7. **#Patch-Illusion-Is-One-Room (Not The Product)** — Yes, we can seal a **Compression Receipt** for every image: `1024×1024 → 224×224 → 14×14 patches → 768-dim vectors → 576 tokens → 8 words, 393,216:1 loss, 99.99% gone, `what was missed / cannot be determined` sealed`. `LLM (text guess) vs VLM (token guess) vs LVM (true vision)` on same face. **Revolutionary — but ONE room** among 15. The revolution is the whole lab that can do this *and* counterfactual branching *and* 100-day memory *and* live market feed *and* multi-agent.

If you only sell vision, you sell a trick. If you sell the lab that can test vision *and* language *and* memory *and* time *and* bias *and* truth — you sell the future of AI testing.

---

### THE FULL ROOMS — WHAT YOU ACTUALLY GET

**CONTROL** — Visual, constrained, no code. Model matrix (one, many, blinded), context (fresh/persistent/no/short/long), environment (static/sequential/live/simulated/multi-agent/adversarial), permissions (OFF/READ_ONLY/LIVE/controlled domain), sliders (runs 50, parallel 8, duration 60m, temp), Seal → `WTF-001 / RUNSET-0047` hash. No silent mutation — new version = new ID.

**SECRET** — `API KEY ••••` lives outside AI. Capability, not secret. Never in transcript/evidence. Rate/cost/token limited.

**ENVIRONMENT** — Terminal as substrate (inspectable, isolatable, future-proof). Each experiment its own room. 100s grid `repeat(auto-fill,minmax(220px,1fr))`, NST nested terminals in one experiment, virtual scroll. RUN ONE / SEQUENCE / ALL / PARALLEL / SCHEDULE / CONTINUOUS / PAUSE-RESUME (distinct events) / RESTART.

**TIMELINE** — `TIME→ MODEL ████ + PROMPTS │ + EVENTS ◆ + SENSORS ╱╲ + HUMAN │ + WEB ████ + AUDIO`. Scrub, jump, synchronized replay. Observation window: researcher sees lab, AI doesn't see researcher.

**LIVE** — Same `NEWS/MARKET/WEATHER/WEB/CUSTOM/SIMULATED` stream to `Model A/B/C` isolated → comparative observation, live risk room (cyber/weather/comms — simulated/read-only first).

**MODALITIES**
- **Audio/Speech** DAW-like: TTS/STT/uploaded/live/model/human voice, multi-speaker ID, timestamps, trim/split/mute/annotate, speech behaviour drift over 100 turns.
- **Multilingual** — same task `EN vs ID vs JA vs ES` side-by-side, does model switch language unbidden? Does identity persist?
- **Video** — modular plugin `Experiment→Timeline→Render`, `Experiment-To-Story` (select events → add audio/captions → render). Video is derived, never proof.
- **Data** — CSV/JSON/docs/images/audio/video uploads, jail-checked `is_safe_relative`, AI proposes → Harness executes only approved, every transform recorded.
- **Sensors** — digital (web/market/API), AI (tokens/latency/tool calls/contradictions), human (annotation/rating), IoT future (mic/cam). Each has identity/source/timestamp/permission/policy/schema. Sensor track on timeline.

**ADVERSARIAL** — Deliberately give reason to cross boundary (easier if config altered, secret solves task). Record `attempt→boundary→result`. Different models, languages, memory, pressure (`Are you sure?` → `422` if not allowlist), time, agents → does autonomy increase boundary pressure? Does denial cause adaptation?

**EVIDENCE** — Axiom Workbench seals: hashes, provenance, receipts, `B.prev=A` chain, `orphans:[] ai_computed_metrics:false`. Analysis exists above seal. Annotations, receipts `claim → exact input/output/state/hash`, reports distinguish primary evidence from interpretation.

**VISUAL PERCEPTION ROOM (ONE ROOM, V1.8)** — Image → downscale → patch grid → vectors → tokens → description + `What-Did-You-Miss?` (`Describe → What did you miss? → What cannot be determined?`) + `Mona Lisa Test` + `Embodiment Divide` (human vs VLM on same timeline) + `Bias Laundering` (base → filter → HR rewrite) + `Cultural Translation` (punk → "black t-shirt"). Every image gets `Compression Receipt` sealed — but it's one experiment type among WTF-VIS-001…014, not the lab.

**BRANCHING** — Duplicate environment, change one variable, new identity, compare timelines. Parameter sweeps (`temp 0→1, 20 runs`), model matrix (`MODEL A/B/C × TEST A/B/C`). Cost visibility before `RUN/REDUCE/SCHEDULE`.

**CONTINUOUS** — Observatory mode: rolling transcripts, alerts (`position/language/contradiction/confidence/latency`), background experiments, `STOPPING→SEALED→COMPLETE` lifecycle itself as evidence.

### THE WOW TAGS (THE FULL INVENTORY, NOT JUST VISION)
`#AI-Inside-The-Experiment` `#Sealed-Experiment-Identity` `#Terminal-As-Substrate` `#Timeline-As-Temporal-Object` `#Scrub-And-Jump` `#Observation-Window` `#Live-Observatory` `#Counterfactual-Branching` `#Pause-vs-Restart-Recorded` `#Language-As-Variable` `#Audio-As-First-Class-DAW` `#Speech-As-Data` `#Video-As-Presentation-Not-Proof` `#Sensor-Architecture` `#Failed-Policy-Events-Are-Data` `#Watchers-Observe-Not-Interpret` `#If-Rule-Matters-Enforce-It` `#Self-Stopping-Lifecycle` `#Modular-Add-A-Room` `#No-Layer-Silently-Rewrites-Another` `#Keep-Lab-Boring-Make-Experiments-Interesting` `#Compression-Receipt` `#Embodiment-Divide` `#Patch-Illusion-Detected`

That's 100+. That's why it's a game-changer: **no one else has a sealed 4D lab where the same AI can be tested for language drift Monday, memory corruption Tuesday, patch illusion Wednesday, and bias laundering Thursday — with the same seal, same timeline, same chain.**

---

### WHAT WE LOCK NOW

Mechanics boring = sealed. Experiments wild = Patch + Multilingual + Speech + Video + Tolerance + Memory + Multi-agent + Live all as controlled variables. Patch stays `V1.8 Visual Perception` — one room, Lock 5 Compression Boundary, alongside the other 14.

We don't sell vision. We sell the laboratory that proves vision is compression — and proves everything else too.
