# CHANGE CONTROL — Axiom Lab Harness — 2026-09-26

**What may change:** `app/*.html` taste no purple + `axiom_harness/mission.py` `frozen Optional` bolt-on + `api/main.py` `POST /master/*` + `tests/test_*.py` + `docs/*.md` governance — never `Axiom-Workbench` engine, never `PER-Core` `ΔH>0.5` maths in `axiom_harness/`.

**Who:** `Carl` (Architect) approves `METHOD_VERSION` change, `PER/ECA ON`, `push live`, `ARCHIVE` move breaking `B.prev=A`.

**When/versioned:** `git log --oneline` split commits per house order §1-6 already `d479fe9 → f024fee` — `git add -p` if one file spans units, never squashed. Old versions stay via `git show HEAD:axiom_harness/mission.py`.

**New RUN vs rerun vs supersession:** `rerun = new RUN_ID` same `METHOD_VERSION`; `changed method = new METHOD_VERSION`; `superseding experiment = new EXPERIMENT_ID` (`EXP-HARNESS-002`).

**Lab vs experiment vs rerun:** Changing lab infra (`SETUP.md`) ≠ changing experiment (`METHOD.md`) ≠ rerunning (`RUN_ID`).

*House Order §23 — no historical hashes altered.*
