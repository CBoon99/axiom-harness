# CURRENT HUMAN READ — 2026-09-26 — Fast Briefing for Carl

**WHAT THIS LAB IS:** Boring, hash-sealed `Axiom Lab Harness` — governs `Axiom-Workbench` + `PER` + `ECA` + `WTF` as bolt-on `frozen Optional` rooms via `POST /master/* 201/422`.

**WHAT IT DOES:** `139 passed` HEAD `f024fee` + `smoke 5/5` + `11-page headless 1280/390` reflow correct + `5-browser` multi-feed.

**WHAT IS PROVEN:** `V1.7 18cell 5-file seal + ECA 66/66` + `V1.8 Multi-agent 6 tests + V1.9 Video 5 + V2 Films 5 + V2+ Observatory+External API 7 + Human V3 5 + Export 3` — all on `origin/main`.

**WHAT IS NOT PROVEN:** Live `world-a.netlify.app` feed wiring, real `PER ΔH>0.5` with 1M live data.

**CURRENT EXPERIMENTS:** `EXP-WTF-001` HISTORICAL, `EXP-HUMAN-001` READY (80 dilemmas).

**CURRENT TESTS:** 27 files, 139 tests, `test_harness_boundary 12` green.

**KNOWN FAILURES:** `world-a` not curl-verified, `VisualConfig` not added (reverted per your guard), `AXIOM_ broad` kept until test migration (G10).

**KNOWN LIMITATIONS:** `platform.mac_ver ''` needs `PYTHONPATH`, `agent-browser` needs `require_escalated`.

**ACTIVE:** House Order governance docs being bolted (§1-6 done).

**BLOCKED:** `PER/ECA ON` until `ReceiptIndex` wired.

**SAFE:** `pytest -q`, `smoke.sh`, `file://` browser on `app/*.html`.

**DANGEROUS:** `AXIOM_STRICT_MODE=0` override, `ARCHIVE` move breaking `B.prev=A`, `push live` without `f024fee` hash check.

**WHERE TO START:** `MASTER.md` → `INDEX.json` → `PROOF/HARNESS_PREFLIGHT_2026-09-26.md`.

**HOW TO RUN/VERIFY:** `PYTHONPATH="$PWD" /Library/Developer/CommandLineTools/usr/bin/python3 -m pytest -q` → `sha256sum -c manifest-sha256.txt`.

**WHAT NOT TO TRUST WITHOUT RECEIPTS:** Any `GateOk/ΔH` claim not in `outputs/` with `RECEIPT_ID`.
