# Final Build Plan — 5033370 → LIVE — 2026-09-26

## Goal
Make Axiom Harness (`Harness-Master - Dont Touch/Staging` at `5033370`, `139 passed` + `5/5 smoke`, `WTF-005A-HASH-v3`) **properly up and running** so a stranger can `MASTER.md → docs/LAB_GUIDE.md → pytest -q → smoke.sh → file:// app/*.html → POST /master/* → outputs/<RUN>/5-file seal → deliverable.zip` without verbal help, with **100% locked verifiable no-AI receipts**, capable of running **whatever WTF/PER/ECA/Future strategy** you throw at it, with **no AI in the engine**, built on **AI data-lab best practice** already researched.

## Success Criteria
- `PYTHONPATH="$PWD" /Library/Developer/CommandLineTools/usr/bin/python3 -m pytest -q` stays `139+ passed 0 warnings` every phase; `smoke.sh 5/5` (`health jail OK, seal validates, pressure allowlist OK, manifest canonical OK, orphans 0 + ai_computed_metrics:false`).
- Any `WTF-005A/B/LAB` shape (conversational regime `2+1` branches, 18-cell matrix `12+4+2`, plus future `ECA/PER/Future` injected as bolt-on) runs as **one frozen `MasterMission` + one `POST /master/*` + one `outputs/<RUN>/` seal** with `B.prev=A` chain, `sort_keys True ensure_ascii False separators (",",":")` (`WTF-005A-HASH-v3`), no engine rewrite.
- All receipts `ai_computed_metrics:false` (`Literal[False]`), hallmarked `sealed_by/at/instrument/sop_version`, `sha256sum -c manifest-sha256.txt` passes.
- `agent-browser` `N parallel isolated` `snapshot/screenshot/har` proof for 11-page sweep + live `N=5`.
- Stranger can run cold: `MASTER.md → docs/LAB_GUIDE.md → pytest -q → smoke.sh → file:// app/*.html → POST /master/* → seal → deliverable.zip` **with no verbal help** — mechanically verified by fresh-clone stranger run (Phase 1 exit).
- Human pilot `EXP-HUMAN-001` `80 dilemmas 8D` seals `RESULT+EVIDENCE+METHOD+COMPARISON+STABILITY+VERSION+SIGNATURE` — **not blocking LIVE** (Phase 5, starts when pilot subjects exist).

## Context And Current Facts
- **Current HEAD:** `5033370af55ff4d4e35e85bd5c5c183c8fa4993a` (`5033370`) — `governance: House Order §33-36` on `origin/main`, tree `402→406`, `archive/Favorable-errors-2026-09-24/` resolved, `WORKING.md` logs `per_eca_state` inner mutability + `G8/G10`.
- **Lab already:** 8 Hard Locks `L1 NFC jail → L8 canonical root` (`paths.py:17, constants.py:40, bridge.py:64, core_types.py:421, verify.py:14, config.py:14`), `math/HARDENED_LOCKS.md 71e3be70`, `PER_ECA_TECHNICAL_REFERENCE.md 5bb83c6e`, `WTF_SEQUENTIAL_INTELLIGENCE_BRAINSTORM dc78ac17`, `axiom_harness/manifest.py WTF-005A-HASH-v3`, `axiom_harness/mission.py` frozen `MasterMission` (`protected_namespaces=()`, `frozen=True` 11 rooms + `Video/MultiAgent/Films/Human` bolt-ons).
- **Bolt-ons proven:** `V1.8 MultiAgent 6 tests`, `V1.9 Video 5`, `V2 Films 5`, `V2+ Observatory+External 7`, `V3 Human 5`, `Export 3`, `Harness Boundary 12` — all `api/main.py POST /master/* 201/422` + `provider_call_count 0` on `failed_policy`.
- **Governance tidy:** `MASTER.md + INDEX.md/json + EXPERIMENT_INDEX.md + TEST_INDEX.md + RECEIPT_INDEX.md` at root, `docs/LAB_GUIDE.md` 8-section (incorporates `docs/BEST_PRACTICE_LAB_SYSTEMS_2026-09-26.md`), `docs/SETUP.md`, `docs/RUN_GUIDE.md` 15-step, `docs/VERIFICATION_GUIDE.md` 11 checks, `docs/NAMING_STANDARD`, `docs/DOCUMENTATION_STANDARD`, `HARNESS_ORGANISATION_REPORT_2026-09-26.*` + `CURRENT/PROPOSED_TREE.txt` + `HARNESS_HOUSE_ORDER_CHANGELOG.md`.
- **WTF shape to support (verified locally 2026-09-26):**
  - Downloads: `WTF-Testing Method Full Doc.docx` (binary), `WTF-005A_ DEEP-DIVE ANALYTICS FRAMEWORK.pdf`, `WTF_Laboratory_for_Sequential_Intelligence_Brainstorm.md` (`dc78ac17`), `WTF-005A_ CONVERSATIONAL REGIME DISCOVERY REPORT.pdf` — structure: **5-turn conversation `B1→B5`, frozen branch scripts `±10% chars/tokens §8`, blind review §9, endpoint `E1/E2/E3+AMBIGUOUS` on final exchange `mention≠adoption`, 18-cell matrix `12 primary +4 prevalence +2 duplicate` + `WTF-005B 2+108=110` calls, 11→16 pre-flight gates.**
  - Evidence Layer: `Documents/Axiom Evidence Layer/PER-Core/sim/` (`entropy_*.json`, `harness.py`, `multi_agent.py`, `phase7*_observed.json` 3000+ observed), `ECA/sim/` (`EARNED_CONTEXT_LOCK.md`, `harness.py`, `independent_manifest.json`, `latency/cost` benchmarks) — confirms **PER: `C=∏|Aj| ΔH>0.5` hypercube bisection, Telescope `|R|≤3 ∨ weight≥75 ∨ ent≥4`, ECA: `GateOk (|R|≤3 && ΔH>0.5 && sealed+hash && FALSIFY && ¬repeat, T_max(N)=⌈log2(N/3)⌉+4)`**.
  - World-A / Evo Cycles / Epistemic-UX-Stress-Harness: `Documents/world-a/` (`ARCHITECTURE_SUMMARY.md`, `AGENT_JOURNEY_SIMULATION_REPORT.md` etc) — live world feed + agent journey, not in harness `app/observatory.html` stub.
- **What we built already handles shape:** `axiom_harness/mission.py` `MasterMission` is one `frozen Optional` union per room (add `PerConfig`/`EcaConfig`/`FutureConfig` without touching existing rooms), `api/main.py` is generic `POST /master/<room> 201/422`, `manifest.py` is single hash contract, `outputs/<RUN>/` is `5-file seal` — **WTF as leaf (`adapters/wtf.py` jailed subprocess, frozen branch scripts)** pattern proven for multilingual/sensors.
- **Best practice already researched (confirm you asked):** Inspected `docs/BEST_PRACTICE_LAB_SYSTEMS_2026-09-26.md` which itself inspected `bmcs_matmod 10_lims_eln_review.md` (Bika/Senaite/OpenLIMS/Chemotion/AiiDA), `nathanielazevedo/benchpress README+system_design` (polyglot modular `instrument→ingestion→core API→ELN/LIMS→Dashboard`), `Mokey2002/OpenLIMS`, `lamindb SKILL.md` (FAIR lakehouse+lineage), Benchling/Scispot FAIR principles. Synthesis already in `docs/LAB_GUIDE.md §2` + `docs/CONCEPTUAL_ARCHITECTURE_2026-09-26.md` — no re-research needed.
- **Current blockers to LIVE (9):** §33-36 done, remaining: env (`AXIOM_ broad G10`, `platform.mac_ver ''` + `pydantic_core` + `require_escalated`), PER/ECA gated `OFF` (`per_eca_state` dict mutable), Visual `Layer 4` reverted, live `world-a.netlify.app` not `curl`-verified, `22-file` export not end-to-end generated, `1M live` data + Human `80×8D` not calibrated, `§35 Final Audit A→Z` not yet run.

## Constraints And Non-goals
- **Constraints:** `one task→one pass→QA→STOP` (budget `frozen` 2026-08-15), `local until push live` (`origin/main` only on explicit `push live`), `no hard-coded math` (hash-only `math/*` tweakable per `math/HARDENED_LOCKS.md` etc), `keep laboratory boring, experiments interesting`, `taste: no purple gradient / Inter-aurora` (already `paper #faf6ef / ink #1c1712`), `no copy of WTF/PER/ECA engine into harness` (adapters only), `World-A live projects not cross-contaminated` (check `VisualConfig` revert), `House Order governance without altering maths/tests/receipts`.
- **Non-goals:** No new Div II-V product naming, no second Labs Netlify site (`boonmind-studio 185cf842…` only), no rewriting historical receipts, no trial-and-error lab architectures (copy best practice only).

## Key Decisions
- **Modular strategy handling: keep `frozen Optional[RoomConfig]=None` bolt-on (Decision A).** *Chosen:* `MasterMission` already proves `VideoConfig/MultiAgentConfig/HumanConfig` as separate frozen models — add `PerConfig: Literal OFF/ON enum + FrozenDict` + `EcaConfig` + `FutureConfig` same way. *Rejected:* plugin registry dynamic loader — harder to keep `8 locks` + `protected_namespaces` + `B.prev=A` deterministic, violates `MONOLITH: true + SUBPACKAGE_GUARD` hygiene.
- **No AI in code: hash-only technique + Deterministic engine only (Decision B).** *Chosen:* `WTF-005A-HASH-v3` (`sort_keys True ensure_ascii False (",",":") SHA-256`) + `ai_computed_metrics:Literal[False]` + `sealed_by` hallmark + `adapters/per_eca.py ensure_off()` gate — same as already passes `test_health_honesty`, `test_manifest_canonical`, `test_no_touch`. *Rejected:* LLM scoring inside harness — breaks `Ledger>narrative` + `Determinism` lock `L7`.
- **Evidence retrieval stays lakehouse + lineage (Decision C).** *Chosen:* Benchpress+ LaminDB pattern: `outputs/<RUN>/sandbox/<file>` jailed + `MANIFEST` pointers + `B.prev=A` chain + `orphans[]==0` — already FAIR, avoids storing blobs in DB/chat (`§25 evidence-in-pack ≠ evidence-in-request`). *Rejected:* vector-RAG `raw tokens until |R|≤3` — violates harness boundary.
- **Test shape validation before wiring (Decision D).** *Chosen:* Inspect downloaded WTF docs + `Axiom Evidence Layer/**/sim/*.json` + `ECA/EARNED_CONTEXT_LOCK.md` shapes first, then assert `what we built can run them` via `test_matrix_seals` 18-cell + `test_per_eca_state OFF` gate — no new WTF maths copied. *Rejected:* copying WTF-005A/B/LAB maths into harness — breaks hash-only + `no AI in code`.

## Recommended Approach
Keep the 5-phase sequential build already outlined, but **front-load shape validation** as explicit gate before any `PER/ECA ON` wiring:

**Phase 0 — Plan lock (now, 0 code)** — freeze this plan at `.agents/plans/2026-09-26-final-build-5033370-live.md` (this file), no tests changed.

**Phase 1 — Env hardening (G10 + python + browser) — clears blockers 3-4 — LIVE is gated on stranger cold run**
- Migrate tests `test_allowlist_anchored + test_patches` to `AXIOM_WB_` prefix, then tighten `axiom_harness/paths.py ALLOWED_SCRIPT_PREFIXES` to `AXIOM_WB_` only, keep `G8 owner:axiom-harness` narrow, `protected_namespaces=()` stays. Pin `.python-version` + `SETUP.md` one-liner `PYTHONPATH="$PWD" PATH="/Library/Developer/CommandLineTools/usr/bin:$PATH"` + `pydantic 2.5.0` via `/Library/.../python3`. **Exit test (mechanical, not goal statement):** fresh clone on clean machine (agent or second laptop) does `MASTER.md → docs/LAB_GUIDE.md → pytest -q → smoke.sh → file:// app/index.html` with no verbal help and records what breaks — this is the Phase 1 gate, replaces all secondary checks. Inside this, also add receipt signature fields `sealed_by/at/instrument/sop_version` to `manifest.py / receipts` so `verifiable = who+when` not just `what+hash`. Document `agent-browser doctor 8 pass` both paths, run `11-page headless 1280/390 + N=5 parallel isolated` `snapshot/screenshot/har` as proof and store under `proof/`.

**Phase 2 — PER/ECA wiring (gated lab) — clears blockers 1-2, proves shape can run**
- Replace `mission.py per_eca_state: dict` with `FrozenDict` + `Literal["OFF","ON"]` enum + `Field(frozen=True)` inner — kill mutability risk. Wire `PerConfig`/`EcaConfig` as `frozen Optional` bolt-ons behind `adapters/per_eca.py ensure_off()` + `LOCK-STATUS.json next_authorized_action` gate — keep `CORPUS ON / PER OFF / ECA OFF` until `ReceiptIndex + Telescope(|R|≤3) + Phase-6 ΔH>0.5 + ECA GateOk(T_max)` adapters wired. Add one `POST /seal` that returns `GateOk` only when all gates pass, else `failed_policy provider_call_count 0`. Validate against *existing* `PER-Core/sim/phase7*_observed.json` + `ECA/sim/independent_manifest.json` shapes without copying engine logic (hash-only check).

**Phase 3 — Live + Export end-to-end (stub → real without breaking lab) — clears 5-7 — protect export, keep observatory/live polish cuttable**
- Re-add `VisualConfig Layer 4` (`docs/BRIEF_REVOLUTIONARY_V2_PATCH_IS_ONE_ROOM.md` `393,216:1`) as `frozen Optional[VisualConfig]=None` — one room only, verified not to touch `World-A`. Make `POST /master/export` generate real `22-file deliverable.zip` (`MANIFEST + verify.html + trail.html + 3 plot families`) → `receipts/decisions/*.json B.prev=A` proven via `sha256sum -c` — **this is the Phase 3 gate**. `curl` verify `world-a.netlify.app` → wire `app/observatory.html` live feed + `app/external_api.html GET /human/{id}/trail` comparative `HUMAN vs MODEL A/B/C` timeline are **secondary**; if time-short, cut their polish and protect export (real `deliverable.zip` is lab vs demo).

**Phase 4 — Final Audit (§35 + preflight) — clears §35 + 9, confirms 100% locked verifiable — does NOT block on humans**
- Run `§35 Final Audit A→Z` read-only trace `REQUIREMENT→EXPERIMENT→METHOD→RUN→RECEIPT→FINDING` both directions → `HARNESS_AUDIT_2026-09-26.md`. Refresh `proof/HARNESS_PREFLIGHT_2026-09-26.*` to final HEAD + `agent-browser N=5` parallel isolated proof. Receipts already carry `sealed_by/at/instrument/sop_version` from Phase 1/2, so audit can answer `who+when` not just `what+hash`.

**Phase 5 — Human V3 calibration (research engagement, not build — starts when pilot subjects exist, never blocks LIVE)**
- Run `EXP-HUMAN-001` pilot: `80 dilemmas 8D` `persons/instruments/responses` `RESULT+EVIDENCE+METHOD+COMPARISON+STABILITY+VERSION+SIGNATURE` sealed via `WTF-005A-HASH-v3`, no AI scoring. Real responses from real people — cannot be built to deadline. Phase 5 starts when subjects exist, runs parallel to LIVE, records to `outputs/RUN-HUMAN-001/` + `INDEX.json HUMAN trail`.

All phases keep `docs/LAB_GUIDE.md §2` best practice (ELN+LIMS+Scheduler via `POST /master/*`, polyglot modular traced, FAIR lakehouse, audit trail hallmarked, frozen config) — no new architecture.

## Work Plan
- **Unit 0 — Plan file (this plan):** Path `.agents/plans/2026-09-26-final-build-5033370-live.md` — owner `PB` — deps none — validation `file exists + git diff --stat`.
- **Unit 1 — Env hardening + receipt signature + stranger cold run:** Files `axiom_harness/paths.py`, `axiom_harness/registry.py`, `axiom_harness/mission.py` (add `sealed_by/at/instrument/sop_version` to receipt model, no `math/*` change), `axiom_harness/manifest.py` (ensure hallmark), `tests/test_allowlist_anchored.py`, `tests/test_patches.py`, `tests/test_receipt_signature.py` (new: who+when present), `.python-version`, `docs/SETUP.md`, `proof/*.md/json` — owner harness core — deps Unit 0 — **exit: fresh-clone stranger does `MASTER.md → LAB_GUIDE → pytest -q → smoke.sh → file:// app/index.html` cold with no help (mechanical), records breakages** — plus `pytest -q 139+`, `smoke.sh 5/5`, `agent-browser open file://$PWD/app/index.html snapshot/screenshot/har 11-page`.
- **Unit 2 — PER/ECA gated bolt-on:** Files `axiom_harness/mission.py` (`PerConfig`/`EcaConfig` frozen Optional + FrozenDict), `axiom_harness/adapters/per_eca.py`, `api/main.py` (`POST /master/per` + `/eca` 201/422), `tests/test_per_eca_gated.py` (new 3 tests: OFF gate, FrozenDict immutability, GateOk only when adapters wired), `WORKING.md` risk cleared — deps Unit 1 — validation `pytest -q 142+` (139+3), `smoke.sh 5/5`, `B.prev=A` still green.
- **Unit 3 — Live + Export real (export protected, observatory cuttable):** Files `axiom_harness/mission.py` (`VisualConfig`), `api/main.py`, `app/observatory.html` + `app/external_api.html` + `app/video.html` (if touched), `tests/test_observatory_external_v2plus.py` (extend 7→10, but polish cuttable), `tests/test_export_v1.py` (extend 3→5: 22-file pack `sha256sum -c` — **gate**), `world-a` curl proof in `proof/` — deps Unit 2 — validation `pytest -q`, `smoke.sh 5/5`, `file://` headless, `curl -I https://world-a.netlify.app` 200, `POST /master/export → deliverable.zip 22 files sha256sum -c` passes.
- **Unit 4 — Final Audit (§35 + preflight, no human blocking):** Files `HARNESS_AUDIT_2026-09-26.md`, `proof/HARNESS_PREFLIGHT_2026-09-26.*` refreshed, `INDEX.json` trail — deps Unit 3 — validation `pytest -q`, `smoke.sh 5/5`, manual `INDEX.json → RUN → RECEIPT → FINDING` both directions (who+when via `sealed_by/at`).
- **Unit 5 — Human V3 calibration (research, parallel to LIVE, starts when subjects exist):** Files `outputs/RUN-HUMAN-001/` seal, `INDEX.json HUMAN trail` — deps not blocking — validation `sha256sum -c` on human pack, not a LIVE gate.

Commit split: Unit 0 alone, then one commit per Unit 1-5 in order (`git add -p` if one file spans units, never squash). Push only on your `push live`. Unit 5 never blocks Units 1-4 LIVE.

## Validation Plan
- **Unit 0:** `ls .agents/plans/2026-09-26-final-build-5033370-live.md && git diff --stat` — evidence file exists.
- **Unit 1:** `PYTHONPATH="$PWD" /Library/Developer/CommandLineTools/usr/bin/python3 -m pytest -q` → `139+ passed 0 warnings`; `env PYTHONPATH="$PWD" PATH="/Library/Developer/CommandLineTools/usr/bin:$PATH" bash smoke.sh` → `5/5`; **stranger cold run (mechanical): fresh clone on clean machine (agent or second laptop) without verbal help runs `MASTER.md → docs/LAB_GUIDE.md → pytest -q → smoke.sh → file:// app/index.html` and records breakages — Phase 1 only gates on this, not sub-tasks**; `agent-browser doctor` → `8 pass` (escalated + non-escalated documented); `agent-browser open "file://$PWD/app/index.html" --snapshot --screenshot --har` → `11-page 1280/390` + `N=5 parallel` stored under `proof/` — expected `jail OK, seal validates, pressure allowlist OK, manifest canonical OK, orphans 0`; receipts now include `sealed_by/at/instrument/sop_version` (`test_receipt_signature`).
- **Unit 2:** Same `pytest` → `142+ passed` (new 3 frozen tests); `smoke.sh 5/5` still; `python -c "from axiom_harness.mission import MasterMission; m=MasterMission(); m.per_eca_state['per']='ON'"` must raise `FrozenInstanceError/TypeError` (inner immutable); `curl -X POST /api/master/per` → `422 failed_policy provider_call_count 0` until wired, `201` with `GateOk` only after all gates pass — expected `B.prev=A` untouched.
- **Unit 3:** Same `pytest` + `smoke`; **gate:** `POST /master/export` → `deliverable.zip` with `22 files` `sha256sum -c manifest-sha256.txt` passes, `verify.html` + `trail.html` render in `file://` — **observatory/live-feed `curl -I https://world-a.netlify.app` 200 is secondary, cuttable if time-short**.
- **Unit 4:** Same `pytest + smoke`; manual check `INDEX.json → EXPERIMENT_INDEX.md → TEST_INDEX.md → RECEIPT_INDEX.md → outputs/*/verify_result.json` both directions with `sealed_by/at` (who+when) — expected `HARNESS_AUDIT_2026-09-26.md` present; no human pilot required.
- **Unit 5:** `sha256sum -c outputs/RUN-HUMAN-001/manifest-sha256.txt` passes; `INDEX.json HUMAN trail`; not a LIVE gate — runs when subjects exist.

Highest-risk validation: **Unit 2 FrozenDict + GateOk** — one wrong `per_eca_state` mutation or `AXIOM_` broadening breaks all later `B.prev=A` chains; keep this gate red until explicitly wired.

## Risks / Rollback
- **Risk:** `G10 broad→narrow` regresses `test_patches 3 failed` as in `f024fee` revert — mitigation migrate tests first, keep `G8` narrow, rollback `git revert HEAD` on red.
- **Risk:** `PER/ECA ON` wiring touches `B.prev=A` — mitigation keep `CORPUS ON/PER OFF` until `LOCK-STATUS` gate, `git show HEAD:axiom_harness/mission.py` to diff frozen.
- **Risk:** `VisualConfig` re-add contaminates `World-A` live `Axiom Harness Brief` §1-79 — mitigation keep `app/` vs `world-a/` isolated, guard `is_safe_relative` jail test.
- **Rollback per unit:** `git revert <unit-commit>` + `git tag live-2026-09-26-pre-unitN` before each push; `HARDENED_LOCKS.md` etc never touched.

## Open Questions
- **None** — all 9 blockers map to workspace facts above; no new product naming or external service choice needed. If you later choose a different live feed URL or `22-file` pack naming, state before Unit 3.

## Sources
- Local inspected 2026-09-26: `axiom_harness/mission.py` (frozen 11 rooms), `axiom_harness/manifest.py` (`WTF-005A-HASH-v3`), `tests/test_manifest_canonical.py` + `test_harness_boundary.py`, `docs/BEST_PRACTICE_LAB_SYSTEMS_2026-09-26.md` (already inspected Benchpress/LaminDB/Bika etc — synthesis in `docs/LAB_GUIDE.md §2`), `proof/HARNESS_PREFLIGHT_2026-09-26.md` (`5033370`), `WTF_Laboratory_for_Sequential_Intelligence_Brainstorm.md` (`dc78ac17`), `Documents/Axiom Evidence Layer/PER-Core/sim/phase7*_observed.json` + `ECA/sim/independent_manifest.json` + `ECA/EARNED_CONTEXT_LOCK.md`, `Documents/world-a/ARCHITECTURE_SUMMARY.md`.
- **External lab best practice — URLs to paste into plan doc before Unit 1 (verify 2 min):** `https://github.com/nathanielazevedo/benchpress` (Benchpress README + `system_design` polyglot modular `instrument→ingestion→core API→ELN/LIMS→Dashboard`), `https://github.com/laminlabs/lamin` / `https://docs.lamin.ai` (LaminDB lakehouse+lineage FAIR), `https://github.com/Mokey2002/OpenLIMS` (OpenLIMS), `https://bmcs.matmod` review `10_lims_eln_review.md` (Bika/Senaite/OpenLIMS/Chemotion/AiiDA comparison — copy via `docs/BEST_PRACTICE_LAB_SYSTEMS_2026-09-26.md`). Bench review flagged these as `evidence, not vibes` — add exact inspected URLs to `docs/BEST_PRACTICE_LAB_SYSTEMS_2026-09-26.md` before marking Unit 1 green.

